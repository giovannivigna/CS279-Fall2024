#include <stdio.h>

int main() {
    int num1 = 10;  // First number
    int num2 = 25;  // Second number
    int sum;

    // Sum the two numbers
    sum = num1 + num2;

    // Print the result
    printf("The sum of %d and %d is: %d\n", num1, num2, sum);

    return 0;
}
